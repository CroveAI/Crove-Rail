# Azure Web App Environment Variables Configuration
# Go to Azure Portal > Your Web App > Configuration > Application Settings
# Add these as key-value pairs:

# Rails Configuration
RAILS_ENV=production
RAILS_LOG_TO_STDOUT=true
RAILS_SERVE_STATIC_FILES=true
RAILS_MAX_THREADS=10
PORT=80

# Security Keys (IMPORTANT: Generate new ones for production!)
SECRET_KEY_BASE=aef1e3ae6252c8f3c60af52292747bb080711bb5bc983551c6e71afec2ffa72f1cf9a93d15abe1243f7eff5b14c666cd1d6a10db1c69ce1e8c5015e5f8431d0a
RAILS_MASTER_KEY=generate_new_master_key_here

# Database Configuration
# Replace with your Azure PostgreSQL connection string
DATABASE_URL=postgresql://[username]@[servername]:[password]@[servername].postgres.database.azure.com:5432/[database]?sslmode=require

# Redis Configuration  
# Option 1: Use Azure Cache for Redis
REDIS_URL=redis://[cache-name].redis.cache.windows.net:6380/0?password=[access-key]&ssl=true

# Option 2: Use free Redis service (for testing)
# REDIS_URL=redis://default:your_password@redis-12345.c1.us-east-1-2.ec2.cloud.redislabs.com:12345

# Application URLs
FRONTEND_URL=https://crove-dev-exdjg2e2esapbee4.southeastasia-01.azurewebsites.net
BACKEND_URL=https://crove-dev-exdjg2e2esapbee4.southeastasia-01.azurewebsites.net
RAILS_RELATIVE_URL_ROOT=/

# OAuth Configuration (if needed)
GOOGLE_OAUTH_CLIENT_ID=your_google_client_id
GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret
FB_APP_ID=your_facebook_app_id
FB_APP_SECRET=your_facebook_app_secret

# Email Configuration (optional)
SMTP_ADDRESS=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
SMTP_PASSWORD=your_sendgrid_api_key
SMTP_DOMAIN=crove.com

# Storage (optional)
ACTIVE_STORAGE_SERVICE=local
# For Azure Blob Storage:
# AZURE_STORAGE_ACCOUNT_NAME=your_storage_account
# AZURE_STORAGE_ACCESS_KEY=your_access_key
# AZURE_STORAGE_CONTAINER=your_container_name

# Startup Command (set in Azure Portal > Configuration > General settings > Startup Command)
# bundle exec rails server -b 0.0.0.0 -p 80